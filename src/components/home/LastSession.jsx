// LastSession.jsx
// One-line summary of the most recent completed session.
// Example: "Upper A · 6 exercises · Tuesday"
// Tapping expands to a compact session detail view.
// Data sourced from sessions table, formatted by the agent's brief.

export default function LastSession({ session }) {
  return null
}
